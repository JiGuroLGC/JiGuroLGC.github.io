## 前言

<img src="https://oss3-bbs.mt2.cn/forum/202507/15/131559x87r2t8b76hnzlfn.png">

屏幕前的各位现在正在浏览这篇帖子时，可能有很大一部分人都用的是 Via 浏览器吧？

我用 Via 已经很多年了，就是喜欢 Via 的小巧精致，和百度、夸克浏览器的臃肿形成了强烈对比，也挽救了我的旧手机。Via 浏览器是真正的"小而美"。

不过，免费又好用的 Via 也有一些缺点，比如我们熟知的白名单限制。众所周知，**Via 浏览器在 4.4.1 版本之后对部分网站进行了白名单限制**，在白名单网站中，资源嗅探等功能被 Via 自行屏蔽。特别是当我们通过嗅探下载一些视频时，这就让人很不舒服。

在网上找了一圈，有关去除 Via 白名单限制的帖子很少，所以我现在打算教大家如何去除。

---

## 一、定位提示来源

我们用 Via 打开 B 站，点击资源嗅探按钮，发现 Via 提示：

> **"该站点不支持资源嗅探"**

<img src="https://icdn.binmt.cc/2507/687519dbb267f.png">

那我们就从这个提示入手。

我们把网断了，**清空 Via 存储**（防止有数据缓存），再次打开 B 站，点击资源嗅探：

<img src="https://icdn.binmt.cc/2507/6875d06c6a68c.png">

发现虽然网页无法加载，但是提示仍正常。于是我们可以判断——

> **该提示是在本地安装包内，并不是通过网络加载的。**

很狗的方法，不过很有用。

---

## 二、在安装包中定位关键代码

接下来，我们提取安装包，用 **MT 管理器** 打开它。

<img src="https://icdn.binmt.cc/2507/6875d135ba158.png">

### 2.1 搜索资源 ID

一般，软件中很多字符串都会在安装包内的 `resources.arsc` 文件中进行声明。

我们打开它，并搜索资源值 `该站点不支持资源嗅探`，发现成功搜到：

<img src="https://icdn.binmt.cc/2507/6875d529e6bd3.png">

我们长按复制资源 ID：

<img src="https://icdn.binmt.cc/2507/6875d5b17654c.png">

再拿到 dex 里进行搜索：

<img src="https://icdn.binmt.cc/2507/6875d5eea683f.png">

<img src="https://icdn.binmt.cc/2507/6875d633067f2.png">

只有一个结果，我们进入并定位：

<img src="https://icdn.binmt.cc/2507/6875d6ae10965.png">

### 2.2 反编译关键方法

为了方便，我们可以把这个方法转成 Java：

```java
package i.a.w;

import d.h.a.e.a;
import i.a.a0.i.g;
import i.a.a0.l.d;

public class za {
    public void r2() {
        a E = E();
        if (E != null && g.a().a(E.a())) {
            bb bbVar = this.a;
            bbVar.l0(bbVar.g0().getString(0x7f1003a0));
            return;
        }
        if (E != null && !d.n(this.a.g0(), E.a())) {
            this.m = E.c();
        }
        int z = z(this.m);
        if (E != null) {
            m.a.a.a("log id: %d, resource id: %d, has resource: %s",
                new Object[]{Integer.valueOf(E.c()), Integer.valueOf(E.D().getId()),
                Boolean.valueOf(i.a.w.rb.d.c().g(this.m))});
        }
        if (z != -1 && i.a.w.rb.d.c().g(this.m)) {
            q2(10, null);
            return;
        }
        this.a.l0(this.a.g0().getString(0x7f10021c)
            + this.a.g0().getString(0x7f100290));
    }
}
```

### 2.3 逻辑分析

这里给大家用伪代码解释一下重要步骤：

> ```text
> // ① 取资源对象
> res = E();
>
> // ② 发现是白名单中的网站
> if (res != null && g.a().a(res.a())) {
>     // 直接给 UI 设置文案 A（0x7f1003a0）并 return；
> }
>
> // ③ 本地未找到资源
> else if (res != null && !d.n(ctx, res.a())) {
>     // 本地缺失，把 logId 记下来
>     this.m = res.c();
> }
>
> // ④ 计算状态码
> int z = z(this.m);
>
> // ⑤ 打一条调试日志（无论后面走哪条路都会打）
> log: logId、iconResId、本地资源包是否存在
>
> // ⑥ 资源包已存在且状态码合法
> if (z != -1 && 本地资源包存在) {
>     q2(10, null);  // 真正去加载/展示
>     return;
> }
>
> // ⑦ 兜底失败提示
> 给 UI 设置文案 B（两段字符串拼接：0x7f10021c + 0x7f100290）
> ```

| 步骤 | 条件 | 行为 |
|:---:|---|------|
| ② | `g.a().a(E.a())` 返回 `true` | 设置"该站点不支持资源嗅探"并返回 |
| ③ | 本地未找到资源 | 记录 logId，继续 |
| ⑥ | `z != -1` 且本地资源包存在 | 调用 `q2(10, null)` 加载资源 |
| ⑦ | 以上都不满足 | 兜底：拼接失败文案（0x7f10021c + 0x7f100290） |

综合分析上述代码，我们发现只要 **`g.a().a(E.a())` 返回 `true`**，Via 就会提示"该站点不支持资源嗅探"。

如果仅仅用 `const` 修改返回值，提示确实会改变，但仍然无法嗅探到资源。我们跳转这些方法，发现很多都是虚拟方法，还有一些并无判断，只是调用了非常多的方法——如果继续找下去，可能很久都找不到结果。

> 很多老哥在这里就已经放弃了，但是为何不换一种方式思考呢。

---

## 三、换切入角度：注入日志记录

其实到现在，**才进入正题**。

我们可以分析得到，如果要进行提示，系统必须判断此刻的网站是否是白名单内的网站。我们不难想到，进行白名单比对最简单的方法是比对网址，如果网址位于白名单内，那么就返回提示语。这点也可以从我们断网但正常提示这点侧面印证。

那么，我们就可以 **以这个为新的切入点**。

### 3.1 初探失败

<img src="https://icdn.binmt.cc/2507/6875dab435964.png">

直接在 dex 中搜索明文 `bilibili`，发现有很多结果。

> （在 `resources.arsc` 里搜索？那你想多了，不可能那么简单）

逐一分析可知，这些都是一些常见网站的声明，与白名单并无明显联系。

<img src="https://icdn.binmt.cc/2507/6875dc8525336.png">

现在估计又有很多人想要放弃了……

> 我们不妨合理猜想：**也许包含 "bilibili" 的白名单可能被某种算法加密了。**

### 3.2 注入日志记录 — 制胜秘诀

我要介绍一种不怎么常用的方式——那就是 **注入日志记录**。

<img src="https://icdn.binmt.cc/2507/6875ddd530ba5.png">

我们点击 Via 原包，再点击"功能"，就可以发现"注入日志记录"这个功能。这是一个被忽视的功能，但它其实就是制胜秘诀。  
我们直接对 Via 注入日志记录，然后安装。

<img src="https://icdn.binmt.cc/2507/6875de0c2e2cb.png">

<img src="https://icdn.binmt.cc/2507/6875de2306770.png">

> **重要：** 一定要给 Via **存储权限**。如果没有权限（可在 `AndroidManifest.xml` 里声明），日志文件就会生成在 `/data/data/mark.via/logs/`，需要 root 权限才能访问。有权限则会生成在 `/storage/emulated/0/MT2/logs/`。

### 3.3 获取并分析日志

现在我们打开 Via，打开 B 站网页端，然后点击资源嗅探按钮，等待提示完全消失。然后打开 MT，定位目录。

这里为了省事，直接定位到 `/data/data/mark.via/logs/`。

<img src="https://icdn.binmt.cc/2507/6875dedcd2af7.png">

可以看到 log 文件已经生成。我们再打开 log 文件，发现有非常多的条目。

不用急——按照我们的猜想，Via 会将网址与白名单比对，即使白名单内容加密，在 log 文件中也一定会有 **解密信息**。

我们直接搜索关键字符 `bilibili`，在排除一些无用条目后，可以迅速地定位到这几行关键信息：

<img src="https://icdn.binmt.cc/2507/6875df7e45ecf.png">

诶，正好。这不就是 Via 提示 **"该站点不支持资源嗅探"** 的几个网站吗？

---

## 四、定位并破解白名单方法

我们通过 log 信息直接定位到方法：

<img src="https://icdn.binmt.cc/2507/6875e0b6d9085.png">

发现这个 `u` 方法定义了几个非常可疑的字符串，再把这几个字符串送去进行 decode——这其实就是进行解密。

有经验的同学一眼就发现，这几个字符串就是非常常见的 **Base64 编码**。通过看字符串最后面的等于号（`=`）和调用方法就可以判断出来。

为了进一步验证我们的猜想，我们用 Base64 转码器对这几个字符串进行转码：

<img src="https://icdn.binmt.cc/2507/6875ef885e5f1.png">

<img src="https://icdn.binmt.cc/2507/6875e19e9db6a.png">

<img src="https://icdn.binmt.cc/2507/6875e19ebb2b9.png">

**转码成功！**而且转码结果正是刚才 log 中提到的 Via 白名单网站。

---

## 五、破解方案

至此，分析过程结束。这个 `u` 方法实际上就是 **对白名单内容进行解密比对的关键方法**。

我们可以采用以下两种方式之一：

| 方案 | 操作 | 效果 |
|:---:|---|------|
| A | 修改 Base64 字符串为任意无效内容 | 解密后得到空/无效域名，白名单失效 |
| B | 清空 `u` 方法，让其直接 `return` 空值 | 彻底跳过白名单比对逻辑 |

保存修改，签名安装。

再次打开 B 站，发现破解成功：

<img src="https://oss3-bbs.mt2.cn/forum/202507/15/021321yecbw3lzberwhen2.png">

---

## 总结

经过这次的修改，相信大家的技术应该都有所提高。主要是为了带大家熟悉一下 **注入日志记录** 这个功能，并分享一下我个人的思路。

感谢大家能够阅读到这里，如果有不同的想法，可在下方评论区分享留言。
